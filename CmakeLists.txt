cmake_minimum_required(VERSION 3.20)
project(lmemp VERSION 1.0.0.1 LANGUAGE CXX)

add_executable(main global_var.hpp 
                    free_list.cc
                    size_class.cc
                    thread_cache.cc
                    span_list.cc
                    central_cache.cc)


target_compile_options(main PRIVATE -pthread)

target_link_libraries(main PRIVATE -pthread)

